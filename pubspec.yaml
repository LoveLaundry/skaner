name: love_mobi
description: Love Laundry Mobile - QR Scanner & Linen Tracker
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  shared_preferences: ^2.2.0
  camera: ^0.10.5+7
  permission_handler: ^11.0.0
  mobile_scanner: ^3.5.5
  flutter_barcode_scanner: ^2.0.0
  provider: ^6.1.0
  intl: ^0.18.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
